import { Router } from 'express';

const router = Router();

// Placeholder routes - implementar después
router.get('/', (req, res) => {
  res.json({
    success: true,
    data: [],
    message: 'Rutas de membresías - pendiente implementación'
  });
});

export default router;
