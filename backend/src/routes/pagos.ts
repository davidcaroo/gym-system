import { Router } from 'express';

const router = Router();

// Placeholder routes - implementar después
router.get('/', (req, res) => {
  res.json({
    success: true,
    data: [],
    message: 'Rutas de pagos - pendiente implementación'
  });
});

export default router;
